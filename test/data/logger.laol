import laol::rt::runtime

interface Logger {
	def info(msg)
	def warn(msg)
	def error(msg)
	def fatal(msg)
}

class FileLogger(logFname) implements Logger {
    override def info(msg) {}
    def m1 {}
}
